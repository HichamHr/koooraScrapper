# Done


# TODO
# Done
1. [ ] Games by date
2. [ ] Game Details

# Run Project Api
* `uvicorn main:app --reload`

#  steps to install
* `python.exe -m pip install --upgrade pip`
* `pip install -r .\requirements.txt`
* `setup.py install`
